# Copy to `.env` (git-ignored) and fill at runtime. NEVER commit real values.
#
# No Canvas/Ally API access yet, so these are placeholders for when tokens land.
# Least-privilege, service/role-owned tokens — not a named student's (plan SEC2).

CANVAS_API_TOKEN=
ALLY_API_TOKEN=
