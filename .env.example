# Copy to `.env` (git-ignored) and fill at runtime. NEVER commit real values.
#
# The offline core currently needs no secrets. If a future stage requires a
# token, add it here (resolved from the environment at runtime, never committed).
